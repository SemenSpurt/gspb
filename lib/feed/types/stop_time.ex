defmodule Feed.Types.StopTime do
  use Ecto.ParameterizedType

  # def type(_params), do: :string

  # def init(opts) do
  #   validate_opts(opts)
  #   Enum.into(opts, %{})
  # end

  # def cast(data, params) do
  #   ...
  #   {:ok, cast_data}
  # end

  # def load(data, _loader, params) do
  #   ...
  #   {:ok, loaded_data}
  # end

  # def dump(data, dumper, params) do
  #   ...
  #   {:ok, dumped_data}
  # end

  # def equal?(a, b, _params) do
  #   a == b
  # end
end
